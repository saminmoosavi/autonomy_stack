; Survey plan for pipeline/missions/factory_survey_02.pddl, as produced by Fast
; Downward (astar(lmcut()), cost 5) against factory_jackal_domain.pddl.
;
; Coverage of R1-R5 and nothing else. Unlike factory_tour_03.txt there is no
; trailing object action to truncate: the survey problem is solvable exactly as
; written, and what happens after it is not planned here at all. The inspections
; are replanned at runtime once perception has reported what there is to
; inspect; those objects do not exist as PDDL symbols until then.
;
; Says nothing about what the robot will find, for the reason the mission file
; gives at length: the seed handed to the LLM must not contain the answer.
;
; TARGET is derived from the last (move ...) by run_sim.sh, making r5 the
; nominal endpoint; the inspection replan retargets it to whichever object it
; ends on.
(move jackal_1 r5 r1)
(move jackal_1 r1 r2)
(move jackal_1 r2 r3)
(move jackal_1 r3 r4)
(move jackal_1 r4 r5)
